steps:
  - task: NuGetToolInstaller@1
    displayName: Install Nuget
    inputs:
      versionSpec: '6.10.0'
  - task: NuGetCommand@2
    displayName: Restore NuGet packages
    inputs:
      command: 'restore'
      restoreSolution: '**/*.sln'
      feedsToUse: 'select'
